# instructions

* run `npm install` to install dependencies
* run the server `npm run start`
* `open localhost:8000`

# bottle modeling

The application models consumption rate at multiple resolutions (month/week/day)
